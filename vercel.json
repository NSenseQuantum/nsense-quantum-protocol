{
  "version": 2,
  "builds": [
    {
      "src": "client/index.html",
      "use": "@vercel/static",
      "config": {
        "outputDirectory": "dist/"
      }
    },
    {
      "src": "server/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "server/**",
          "shared/**"
        ],
        "excludeFiles": [
          "attached_assets/**",
          "client/**",
          "node_modules/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/client/index.html"
    }
  ]
}
